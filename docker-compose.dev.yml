services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: nexotek_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: devpassword
    ports:
      - "5433:5432"
    volumes:
      - nexotek_dev_db:/var/lib/postgresql/data

volumes:
  nexotek_dev_db:
