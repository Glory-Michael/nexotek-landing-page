'use client';

import React from 'react';

export default function Icon() {
  return (
    <span
      style={{
        fontSize: '18px',
        fontWeight: 800,
        letterSpacing: '-0.05em',
        color: 'var(--theme-text)',
      }}
    >
      N
    </span>
  );
}
